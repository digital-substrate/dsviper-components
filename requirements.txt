# Runtime dependencies for dsviper-components — shared Qt Widgets
# library for the dsviper Python ecosystem.

# dsviper runtime + Python binding (used by ds_settings.py and others)
dsviper>=1.2.10

# Qt UI framework. Pinned to guarantee reproducible regeneration of
# dsviper_components/ui_*.py and resources_rc.py via dev/build.py —
# different pyside6-uic versions silently produce different generated
# files (phantom diffs). Must match the version pinned by consumers
# (dsviper-tools, ge-py).
PySide6==6.10.1
